[storage]
use_database = True
local_drive =
local_dir =

[logging]
prefix =
suffix =
extension = log
log_level = info
directory = /tmp/pdl

[project]
name = 500px
catalog_parse = PDL.images.download_base.CatalogPage
image_contact_parse = PDL.images.download_base.ImageContactPage

